"""Concrete workflow definitions.

Each module here exposes ONE constant `DEFINITION: WorkflowDefinition`.
Definitions are scaffolding — they declare states/transitions only, and have
no executable steps yet.
"""
